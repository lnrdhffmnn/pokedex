---
import type { Pokemon } from "@/types";
import { Image } from "@astrojs/image/components";

export interface Props {
  pokemon: Pokemon;
}

const { pokemon } = Astro.props;
---

<div
  class="w-fit p-4 mx-auto bg-white dark:bg-zinc-800 rounded-md border border-zinc-200 dark:border-zinc-700 shadow-sm"
>
  <Image
    src={pokemon.image}
    alt={pokemon.name}
    width={200}
    height={200}
    format="webp"
    draggable={false}
    class="drop-shadow-sm"
  />
  <h2>{pokemon.name}</h2>
</div>
